// @vitest-environment jsdom

it.todo(
  "Grid height should be divisible by its width OR its width should be divisible by its height",
  function () {
    expect.hasAssertions();
    // Get grid height and width (from state or UI?) and check if it's cleanly divisible
    // expect().toBe();
  }
);

it.todo(
  "Grid height should correspond to the pattern height state variable (input by the user or default)",
  function () {
    expect.hasAssertions();
    // expect().toBe();
  }
);

it.todo(
  "Grid width should correspond to the pattern width state variable (input by the user or default) ",
  function () {
    expect.hasAssertions();
    // expect().toBe();
  }
);

// Custom height & width from PatternForm
it.todo("User can set a custom grid size ", function () {
  expect.hasAssertions();
  // expect().toBe();
});
